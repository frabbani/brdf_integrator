// https://en.wikipedia.org/wiki/Specular_highlight

// lambertian BRDF
// out = 1

// phong BRDF
n = 8				// specular power
out = rdotl()**n	// both '**' and 'pow()' work

// gaussian BRDF
// m = 0.5
// w = acos( rdotl() ) / m
// out = exp( -( w * w ) )