// https://en.wikipedia.org/wiki/Specular_highlight

// lambertian BRDF
// out = 1

// phong BRDF
n = 8				// specular power
out = dotl()**n	// both '**' and 'pow()' work

// gaussian BRDF
// m = 0.5
// w = acos( dotl() ) / m
// out = exp( -( w * w ) )